# React Fibre
```
we can pause abort or reuse work also the ability to  assign priority to different types of updates by this features.This is also like hydration concept ->the process of injecting JS in the web for accessing several functionality like button etc is called hydration.
```
# Reconiliation
```
Reconciliation is the algorithm behind what is popularly understood as the "Virtual DOM"
```
# Interview Questions
 ```
 1.suppose there is and array we have to loop and bring the values in list/button/paragraph/anywhere.
 ->Here a concept come in the picture why we should introduce keys?
 ->We have to use keys because of lists is performed using keys.Keys should be stable,predictable and unique.
 ```